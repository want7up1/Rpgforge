[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
cache_dir = "/tmp/rpgforge-pytest-cache"

[tool.ruff]
line-length = 100
target-version = "py311"
cache-dir = "/tmp/rpgforge-ruff-cache"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
